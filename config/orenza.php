<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Pagination
    |--------------------------------------------------------------------------
    |
    | Размер страницы по умолчанию и верхняя граница, которую клиент не может
    | превысить параметром per_page.
    |
    */

    'pagination' => [
        'per_page' => (int) env('PAGINATION_PER_PAGE', 15),
        'max_per_page' => (int) env('PAGINATION_MAX_PER_PAGE', 100),
    ],

];
